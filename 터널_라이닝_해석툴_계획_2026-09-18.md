# 터널 라이닝 해석 툴 계획 (2026-09-18)

교량 파이프라인(도면 → NX API 모델링 → 해석 → 검토 → 5장)의 뼈대를 그대로 쓰고, 부품(판독기·모델 생성기·검토식·보고서 템플릿)만 터널용으로 바꾼다.

## 0. 결론부터

- 모델은 **빔-스프링(beam-spring)**: 라이닝을 보 요소로, 지반을 절점별 **압축전용 지반스프링**(Elastic Link COMP)으로 두는 2D 프레임. CIVIL NX로 충분하고 진단 실무 표준이다. GTS NX 연속체 해석은 2차.
- 진단 툴이므로 설계값이 아니라 **조사값**이 입력이다: GPR 실측 두께·배면공동, 코어/반발경도 강도, 균열 구간. 이 세 가지를 모델에 반영하는 것이 교량과 다른 핵심.
- 판정은 무근 라이닝이면 허용응력(휨압축·휨인장), 철근 라이닝이면 강도설계 P-M. 기준은 터널설계기준(KDS 27) + 도로설계편람 제6편 터널 + 시설물 세부지침(터널). 이 원문이 아직 references에 없다 → 0단계에서 확보.
- 첫 대상은 대표님 과업 중 **읍애터널(동면-주암, 정기점검 이력 있음)** 또는 대표님이 지정하는 NATM 도로터널 1개소. 설계 당시 구조계산서가 있으면 그 값을 재현하는 것을 1차 검증으로 삼는다.

## 1. 재사용하는 것 (레포 tools/)

| 부품 | 상태 |
|---|---|
| NX REST 래퍼, MCT 생성·임포트, ANAL, 결과표(BEAMFORCE·REACTION) | 그대로 |
| DWG→DXF, 문자·좌표 추출, dxf_render 크롭 | 그대로 |
| 하중조합·풀이문장(derive)·부록 xlsx·hwpx 편집·쪽 배치 | 그대로, 조합표만 교체 |
| 자중 검증 루틴(NX DEAD vs 도면 계산) | 라이닝 자중으로 그대로 |
| Elastic Link MCT | GEN만 있음 → **COMP(압축전용)** 추가, 실물 MCT 내보내기로 문법 대조 필수 |

## 2. 새로 만드는 것

### 2-1. 판독기 `tools/tunnel_geom.py`
- 라이닝 표준단면도에서: 내공 중심·반경(3심원 R1·R2·R3), 라이닝 두께(천단·측벽·인버트), 인버트 유무, 측벽 기초폭, 굴착선(지보패턴별).
- 종단면·지보패턴표에서: 토피(H), 지반등급(Ⅰ~Ⅴ), 패턴별 연장.
- 출력 `터널_제원서.json`: 단면 좌표(외측·내측 곡선), 두께 함수 t(θ), 재료, 토피, 지반정수(E, ν, γ, c, φ — 지반조사보고서에서, 없으면 확인 필요).
- 진단 조사값 입력 `조사_제원서.json`: GPR 두께 t_meas(θ, 스팬), 배면공동 구간(θ 범위·깊이), 강도 f_ck,meas, 균열 위치.

### 2-2. 모델 생성기 `projects/<터널>/lining_model.py`
- 아치를 약 5° 간격 보 요소로 분할(3심원 각 구간별), 인버트 있으면 폐합.
- 절점마다 반경방향 스프링(COMP) + 접선방향 스프링(반경의 1/3, 옵션). 스프링 상수 k_r = E·b·ℓ/(R(1+ν)) [터널설계기준·편람 6편, 원문 대조 후 확정].
- 배면공동 구간은 스프링 제거, 두께는 실측값으로 요소별 단면 부여(변단면 아님, 구간별 단면).
- 하중 케이스: 자중(DEAD), 이완하중(Terzaghi, 토피와 굴착폭·지반등급 함수; 얕은 토피는 전토피), 수압(배수형은 잔류수압 또는 0, 비배수형은 정수압), 편압(지형 경사 시), 온도·건조수축(옵션), 활하중(개착·복개는 도로 활하중).
- 조합: 진단 원칙대로 2010/현행 강도·허용응력 체계, 지진 제외. 조합표는 원문 확인 후 확정.
- 자중 검증: NX DEAD 반력 합 = 라이닝 단면적×γ×단위폭.

### 2-3. 검토 `projects/<터널>/lining_check.py`
- 요소별 N·M → 무근: σ = N/A ± M/Z, 허용 휨압축·휨인장과 비교, 편심 e = M/N 과 t/6·t/3 판정. 철근: P-M 상관도(pier_model3의 pm 함수 재사용).
- 균열·공동 구간의 응력 증가율을 "설계 상태 vs 조사 상태" 두 모델로 나란히 산출(진단 결론의 근거).
- 출력 `runs/<터널>/<스팬>_result.json`, 풀이 문장, 부록 xlsx.

### 2-4. 보고서
- 터널 진단 보고서 5장 템플릿(hwpx) 필요 — 대표님 보유 보고서 중 하나를 원본으로. 교량과 같은 방식(XML 복제·값 교체).
- 삽도: 단면 판독 그림, 빔-스프링 모델도, 하중도, NX 모멘트도 캡처, 응력 분포도.

## 3. 단계와 기간

| 단계 | 내용 | 기간 |
|---|---|---|
| 0 | 자료 확보: 대상 터널 도면·지반조사·조사결과, 설계 구조계산서, 기준 원문(KDS 27, 편람 6편, 세부지침 터널), 5장 템플릿 | 반나절 (대표님) |
| 1 | 손으로 쓴 제원서 JSON으로 빔-스프링 한 바퀴: MCT → NX → N·M → 자중 검증. 설계 구조계산서 값 재현 | 1일 |
| 2 | 판독기: 표준단면도·패턴표 → 제원서 자동 | 1~2일 |
| 3 | 조사값 반영: 실측 두께·공동·강도 → 두 모델(설계/조사) 비교 | 1일 |
| 4 | 검토식·조합·풀이·부록·5장 조립·렌더 점검 | 2일 |
| 5 | 두 번째 터널로 재사용성 확인 | 반나절 |

## 4. 대표님 결정 필요

1. 첫 대상 터널(읍애터널? 다른 곳?)과 자료 위치.
2. 라이닝 형식: 무근/철근, 인버트 유무, 배수형/비배수형.
3. 모델 방식: 빔-스프링(CIVIL NX)으로 시작하는 데 동의하는지. GTS NX 보유 여부.
4. 이완하중 산정법(Terzaghi 기본) 과 수압 취급.
5. 판정 체계: 무근 허용응력 기준값 출처(터널설계기준 vs 편람) — 원문 확보 후 대조표로 다시 여쭘.
6. 5장 템플릿으로 쓸 기존 터널 진단 보고서.

## 5. 위험

- 기준 원문이 없으면 스프링 상수·이완하중·허용응력 값을 지어낼 위험 → 0단계 전에는 계산기 작성을 시작하지 않는다.
- 압축전용 링크는 반복 해석이라 수렴 실패 가능 → 초기값·반복 횟수 확인 루틴 필요.
- 3심원 도면의 중심 좌표는 표기 방식이 설계사마다 달라 판독기 규칙이 1회는 손봐야 한다.
